# Bootstrap 5 Audit: DEPRECATED — Migration to Tailwind in progress

This document previously listed audit tasks for our Bootstrap-based UI. The project is now migrating to Tailwind CSS; use this document as a historical reference only.

Recommended follow-ups:
- Create a Tailwind migration checklist (components -> pages) and map each Bootstrap component to a Tailwind equivalent.
- Prioritize navigation, cards, forms, and admin queue components for migration.
- Add visual regression and accessibility tests for critical pages.

Once the migration is complete, archive or remove this Bootstrap audit document.