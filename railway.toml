# Railway-specific deployment configuration
[build]
builder = "nixpacks"

[deploy]
healthcheckPath = "/up"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[[services]]
name = "web"

[services.variables]
RAILS_ENV = "production"
RACK_ENV = "production"
RAILS_SERVE_STATIC_FILES = "enabled"
RAILS_LOG_LEVEL = "info"
PORT = "8080"

[services.build]
buildCommand = "./bin/railway-setup && bin/rails assets:precompile"

[services.start]
command = "bundle exec puma -C config/puma.rb -p ${PORT:-8080}"